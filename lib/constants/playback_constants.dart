/// Playback mode constants for the music player.
enum PlayMode {
  /// Play current track once and stop.
  single,

  /// Loop the current track.
  singleLoop,

  /// Play all tracks in order, stop at end.
  sequence,

  /// Loop the entire playlist.
  loop,

  /// Shuffle the playlist.
  shuffle,
}
